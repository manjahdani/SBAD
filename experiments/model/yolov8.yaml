weights: "${project_dir}/yolov8/yolov8n.pt"
name: "${dataset.name}_yolov8n_${strategy.name}"
entity: "YourEntity" #TO CHANGE for your WANDB entity
project: "${dataset.basename}"
epochs: 100 #Default parameter used in the paper L3D-IVU
batch: 16 #Default parameter used in the paper L3D-IVU
data: "${project_dir}/experiments/model/data.yaml"
